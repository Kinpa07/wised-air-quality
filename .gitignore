# ── Go ──
build/
*.test
*.out
*.exe
# vendor/ is committed on purpose for offline, reproducible builds

# ── SQLite / local data ──
test.db
*.db-journal

# ── Node / Frontend ──
node_modules/
frontend/dist/
.vite/
*.tsbuildinfo

# ── Environment / Secrets ──
.env
.env.local
!.env.example

# ── IDE / OS ──
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ── Local-only (not part of the submission) ──
docs/
NOTES.md
.claude/
/CLAUDE.md
wised-interview-simulator.tar
HANDOFF.md
.playwright-mcp/
